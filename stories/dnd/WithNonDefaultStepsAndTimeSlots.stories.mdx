import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';

import customComponents from '../helpers/customComponents';
import { DragAndDropCalendar, events } from '../helpers';

<Meta
  title="dnd/With non-default steps and timeslots"
  component={DragAndDropCalendar}
/>

export const Template = props => <DragAndDropCalendar {...props} />;

<Canvas>
  <Story
    name="With non-default steps and timeslots"
    args={{
      defaultView: 'week',
      events,
      step: 15,
      timeslots: 4,
      onEventDrop: action('event dropped'),
      onEventResize: action('event resized'),
    }}
    argTypes={{}}
  >
    {Template.bind({})}
  </Story>
</Canvas>
