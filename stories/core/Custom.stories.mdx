import { Canvas, Meta, Story } from '@storybook/addon-docs';

import customComponents from '../helpers/customComponents';
import { events, Calendar } from '../helpers';

<Meta title="Core/Custom components" component={Calendar} />

export const Template = props => <Calendar {...props} />;

export const CustomTimeGutter = () => (
  <p style={{ color: 'brown' }}>Custom gutter text</p>
);

## Custom month date header component

```jsx dark
components={{
  month: {
    dateHeader: ({ label }) => <span>{label} - Custom date header</span>
  }
}}
```

<Canvas>
  <Story
    name="Month date header"
    args={{
      defaultView: 'month',
      views: ['month'],
      events,
      components: {
        month: {
          dateHeader: ({ label }) => <span>{label} - Custom date header</span>,
        },
      },
    }}
    parameters={{
      controls: { disable: true },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Custom week header component

```jsx dark
components={{
  week: {
    header: CustomWeekHeader
  }
}}
```

<Canvas>
  <Story
    name="Week header"
    args={{
      defaultView: 'week',
      views: ['week'],
      events,
      components: {
        week: {
          header: customComponents.header,
        },
      },
    }}
    parameters={{
      controls: { disable: true },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Custom eventWrapper component

```jsx dark
components={{
  week: {
    eventWrapper: CustomEventWrapper
  }
}}
```

<Canvas>
  <Story
    name="eventWrapper"
    args={{
      defaultView: 'day',
      events,
      components: {
        eventWrapper: customComponents.eventWrapper,
      },
    }}
    parameters={{
      controls: { disable: true },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Custom timeGutterHeader component

```jsx dark
components={{
  timeGutterHeader: CustomTimeGutter
}}
```

<Canvas>
  <Story
    name="timeGutterHeader"
    args={{
      defaultView: 'week',
      views: ['week', 'day'],
      events,
      components: {
        timeGutterHeader: CustomTimeGutter,
      },
    }}
    parameters={{
      controls: { disable: true },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Custom timeGutterHeader component

```jsx dark
components={{
  dateCellWrapper: CustomDateCellWrapper
}}
```

<Canvas>
  <Story
    name="dateCellWrapper"
    args={{
      defaultView: 'month',
      views: ['month'],
      events,
      components: {
        dateCellWrapper: customComponents.dateCellWrapper,
      },
    }}
    parameters={{
      controls: { disable: true },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Custom timeSlotWrapper component

```jsx dark
components={{
  timeSlotWrapper: CustomTimeSlotWrapper
}}
```

<Canvas>
  <Story
    name="timeSlotWrapper"
    args={{
      defaultView: 'week',
      views: ['week', 'day'],
      events,
      components: {
        timeSlotWrapper: customComponents.timeSlotWrapper,
      },
    }}
    parameters={{
      controls: { disable: true },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
